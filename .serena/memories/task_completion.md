# When a Task is Completed

1. `make format` — форматирование (gci + gofumpt)
2. `make lint` — линтинг (betteralign + golangci-lint)
3. `make test` — тесты
4. `go build ./...` — убедиться что проект собирается
